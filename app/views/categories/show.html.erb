<h1 class="text-center mt-4">Category: <%= @category.name %></h1>

<div class="text-center mt-4">
  <% if logged_in? %>
    <% if @user == current_user.admin? %>
      
    <% end %>
    <% if current_user.admin? %>
      <div class="buttons">
        <%= button_to "Edit category name", edit_category_path(@category), class: "btn btn-primary mb-1" %>
        <%= button_to "Delete category", category_path(@category), method: :delete, onclick: "return confirm('Do you really want to delete this category?')", class: "btn btn-outline-primary" %>
      <div>
      
    <% end %>
  <% end %>
</div>
<div class="page">
  <h3 class="text-center mt-4">Articles</h3>
  <div class="apple_pagination">
      <%= will_paginate @articles, :container => false %>
    </div>
  <%= render 'articles/article' %>
  <div class="apple_pagination">
      <%= will_paginate @articles, :container => false %>
    </div>
</div>